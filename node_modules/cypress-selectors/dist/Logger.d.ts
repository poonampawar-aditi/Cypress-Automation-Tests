declare type LogLevel = 'info' | 'warning' | 'error';
declare const Logger: {
    LOG_PREFIX: string;
    logSelector: (selector: string, propertyPath: string, level?: LogLevel) => void;
    appendLogPrefix: (message: string) => string;
    log: (message: string, level: LogLevel) => void;
};
export { Logger };
